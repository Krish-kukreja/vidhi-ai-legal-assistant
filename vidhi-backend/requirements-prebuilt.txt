# VIDHI Backend - Pre-built Wheels Only (No Compilation)
# Install these packages that have pre-built Windows wheels

# Core FastAPI (older version with pre-built wheels)
fastapi==0.100.0
uvicorn==0.23.0
python-multipart==0.0.6
pydantic==1.10.12
python-dotenv==1.0.0

# AWS SDK (always has wheels)
boto3==1.34.34

# LangChain (older version)
langchain==0.0.200

# Document processing (skip lxml, use html.parser instead)
beautifulsoup4==4.12.3
requests==2.31.0
httpx==0.24.1

# Utilities
python-dateutil==2.8.2
pytz==2024.1
python-json-logger==2.0.7

# Note: This minimal setup is enough to run VIDHI backend
# Some features may be limited but core functionality works
