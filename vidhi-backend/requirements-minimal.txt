# VIDHI Backend - Minimal Working Setup for Python 3.12+
# This is the absolute minimum to run VIDHI

# FastAPI
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.6
python-dotenv==1.0.0

# AWS SDK
boto3==1.34.34

# Pydantic (compatible with Python 3.12)
pydantic==2.5.0
pydantic-core==2.14.1

# HTTP
requests==2.31.0
httpx==0.26.0

# Document parsing
beautifulsoup4==4.12.3

# Utilities
python-dateutil==2.8.2
pytz==2024.1
python-json-logger==2.0.7

# LangChain - SKIP for now, we'll use direct AWS SDK
# langchain causes Python 3.12 compatibility issues
