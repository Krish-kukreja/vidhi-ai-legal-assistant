# VIDHI Backend Requirements
# AWS-compatible version (replaces Google services)

# FastAPI and server
fastapi>=0.110.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic
python-dotenv==1.0.0

# AWS SDK
boto3==1.34.34
botocore==1.34.34

# LangChain and RAG
langchain>=0.2.0,<0.3.0
langchain-community>=0.2.0,<0.3.0
langchain-core>=0.2.0,<0.3.0
langchain-aws>=0.1.0,<0.2.0

# Vector store
chromadb==0.4.22

# Embeddings (AWS Bedrock + HuggingFace fallback disabled for cloud to save memory)
# sentence-transformers avoided due to PyTorch memory constraints on AppRunner

# Document processing
beautifulsoup4==4.12.3
selenium==4.17.2
lxml==5.1.0

# HTTP requests
requests==2.31.0
httpx==0.26.0

# JSON processing
# jq==1.6.0  # Not needed - jq is a command-line tool, not a Python package

# Utilities
python-dateutil==2.8.2
pytz==2024.1

# For AWS Lambda deployment (optional)
mangum==0.17.0

# Logging and monitoring
python-json-logger==2.0.7

# Testing (optional)
pytest==7.4.4
pytest-asyncio==0.23.3

# Development tools (optional)
black==24.1.1
flake8==7.0.0
mypy==1.8.0
