# VIDHI Backend - Windows Minimal Requirements
# Install these first - all have pre-built wheels for Windows

# Core FastAPI (essential)
fastapi==0.100.0
uvicorn==0.23.0
python-multipart==0.0.6
python-dotenv==1.0.0

# AWS SDK (essential - always has wheels)
boto3==1.34.34

# HTTP requests (essential)
requests==2.31.0
httpx==0.24.1

# Utilities (essential)
python-dateutil==2.8.2
pytz==2024.1
python-json-logger==2.0.7

# Document processing (essential - use html.parser instead of lxml)
beautifulsoup4==4.12.3

# Pydantic v1 (compatible with Python 3.13)
pydantic==1.10.15

# OPTIONAL: Install these AFTER the above work
# Uncomment and install one by one if needed:

# LangChain (may need newer pydantic)
# langchain==0.0.200
# langchain-community==0.0.200

# Vector store (needs compilation)
# chromadb==0.4.22

# Embeddings (large download)
# sentence-transformers==2.3.1
