# VIDHI Backend Requirements - Simplified (No Rust/Cargo needed)
# Use this if you get Cargo/Rust errors

# FastAPI and server
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.5.3
python-dotenv==1.0.0

# AWS SDK
boto3==1.34.34

# LangChain (core only, no community packages that need Rust)
langchain==0.1.6
langchain-core==0.1.23

# Document processing
beautifulsoup4==4.12.3
selenium==4.17.2
lxml==5.1.0

# HTTP requests
requests==2.31.0
httpx==0.26.0

# Utilities
python-dateutil==2.8.2
pytz==2024.1

# For AWS Lambda deployment (optional)
mangum==0.17.0

# Logging
python-json-logger==2.0.7

# Note: ChromaDB and sentence-transformers are OPTIONAL
# VIDHI works fine with just AWS Bedrock embeddings
# If you need them, install Rust first: https://rustup.rs/
