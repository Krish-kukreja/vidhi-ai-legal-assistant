# VIDHI Backend - Ultra Minimal (Guaranteed to work on Python 3.13)

# Core FastAPI - older versions that definitely work
fastapi==0.95.2
uvicorn==0.22.0
python-multipart==0.0.6
python-dotenv==1.0.0

# AWS SDK (always works)
boto3==1.34.34

# HTTP requests
requests==2.31.0

# Utilities
python-dateutil==2.8.2

# Skip pydantic entirely - FastAPI 0.95.2 has compatible version built-in