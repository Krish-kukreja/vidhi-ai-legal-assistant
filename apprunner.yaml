version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install -r vidhi-backend/requirements.txt
run:
  command: sh -c "cd vidhi-backend && uvicorn app:app --host 0.0.0.0 --port 8000"
